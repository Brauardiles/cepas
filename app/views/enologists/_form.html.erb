<%= form_with(model: enologist, local: true) do |form| %>
  <% if enologist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enologist.errors.count, "error") %> prohibited this enologist from being saved:</h2>

      <ul>
      <% enologist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :nationality %>
    <%= form.text_field :nationality %>
  </div>


  <div class="field">
    <%= form.label :magazines %>
    <%= form.collection_select :magazine_id, Magazine.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :positions %>
    <%= form.collection_select :position_id, Position.all, :id, :name %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
