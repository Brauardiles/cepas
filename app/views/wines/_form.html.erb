<%= form_with(model: wine, local: true) do |form| %>
  <% if wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
      <% wine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <!-- <div class="field">
    <%# <%= form.label :Strain %> %>
    <%# <%= form.collection_check_boxes(:strain_ids, @strains, :id, :name) do |b| %> %>
      <%# <%= b.label class: 'label-checkbox' do %>
        <%# <%= b.check_box + b.text %> %>
      <%# <% end %> %> %>
    <%# <% end %> %>
  </div> -->

  <div class="row">
    <%= form.fields_for :assemblies do |assemblies_form| %>
        <%= assemblies_form.label "Porcentaje Cepa" %>
        <%= assemblies_form.hidden_field :strain_id %>
        <%= assemblies_form.number_field :percentage %>
    <% end %>
  </div>

  <div class="row">
    <%= form.fields_for :ratings do |rating_form| %>
        <%= rating_form.label "Nota" %>
        <%= rating_form.number_field :rating %>
    <% end %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
